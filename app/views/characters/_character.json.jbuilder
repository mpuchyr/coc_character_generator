json.extract! character, :id, :owner_id, :first_name, :last_name, :str, :con, :siz, :dex, :app, :pow, :edu, :luck, :age, :damage_bonus, :hp, :mov, :pronoun, :max_sanity, :occupation_id, :mp, :created_at, :updated_at
json.url character_url(character, format: :json)
